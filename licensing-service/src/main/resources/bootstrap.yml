spring:
  application:
    name: licensingservice
  profiles:
    active:
      default
  cloud:
    config:
      url: http://localhost:8888
#      enabled: true
#      discovery:
#        enabled: true
#        serviceId: configserver
